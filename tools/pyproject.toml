[project]
name = "audiolex-tools"
version = "0.1.0"
description = "Offline corpus tooling for AudioLex (TTS generation etc.), not part of the app build."
requires-python = ">=3.13"
dependencies = [
    "piper-tts>=1.4.2",
]
